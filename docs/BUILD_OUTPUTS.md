# Generated build outputs

`v1setup` writes one R1CS, proving key, verifying key and Solidity verifier per circuit. `manifest.json` records constraint counts and SHA-256 values for all artifacts. Generated keys and verifier source are created during CI and Docker startup rather than treated as handwritten source.
