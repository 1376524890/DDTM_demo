# Integration summary

V1 joins generated Groth16 verifiers, the escrow state machine, a proof service, MinIO, PostgreSQL, a reorg-aware indexer and an authenticated gateway. The executable smoke test is the principal end-to-end demonstration.
