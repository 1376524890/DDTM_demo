# V1 implementation scope

V1 is complete when CI passes circuit, real-proof EVM, gateway and Compose checks and the integrated smoke script reaches `CONFIRMED` after decrypting the MinIO ciphertext. This document does not mark those checks as passed; the workflow and smoke output are the source of truth.
