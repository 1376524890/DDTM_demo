# Local credentials

The values in `.env.example` and the standard Hardhat mnemonic are intentionally public development credentials. They make artifact execution deterministic and must never be used for public endpoints, valuable data or real settlement assets.
