# Implemented quality metrics

V1 proves three syntactic metrics over four fixed records: minimum present-record count, maximum numeric value and maximum age relative to an agreed `asOfTime`. The quality commitment contains the count derived inside the circuit, so the prover cannot supply an unrelated quality score. Semantic accuracy remains outside the circuit and enters evidence review when disputed.
